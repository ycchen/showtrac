<%= form_for(@pregnancy) do |f| %>
  <% if @pregnancy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pregnancy.errors.count, "error") %> prohibited this pregnancy from being saved:</h2>

      <ul>
      <% @pregnancy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dog %><br>
    <%= f.select(:dog_id, Dog.bitches.all.map{|b|[b.name, b.id]}, include_blank: 'Select a Bitch')%>
  </div>
  <div class="field">
    <%= f.label :heat_start_date %><br>
    <%= f.date_select :heat_start_date %>
  </div>
  <div class="field">
    <%= f.label :Estimate_due_date %><br>
    <%= f.date_select :due_date %>
  </div>
  <div class="field">
    <%= f.label :Estimate_surgery_date %><br>
    <%= f.date_select :surgery_date %>
  </div>
  <div class="field">
    <%= f.label :total_puppy %><br>
    <%= f.number_field :total_puppy %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
