<div class="navbar navbar-inverse navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container">
			<button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">	
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="brand" href="/">DogShowTrac</a>
			<div class="nav-collapse collapse">
		        
		        <ul class="nav">
		        	<li><a href="<%= "#{dog_expenses_path}" %>">Expenses</a></li>
		        	<li><a href="<%= "#{sales_path}" %>">Sales</a></li>
		        	<li class="dropdown">
			              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Management <b class="caret"></b></a>
							<ul class="dropdown-menu">
								<li><a href="<%= "#{dogs_path}" %>">Dogs</a></li>
								<li><a href="<%= "#{colors_path}" %>">Colors</a></li>
		          				<li><a href="<%= "#{shows_path}" %>">Shows</a></li>
								<li><a href="<%= "#{charges_path}" %>">Charges</a></li>
								<li><a href="<%= "#{payment_types_path}" %>">Payment Types</a></li>
								<li><a href="<%= "#{payment_statuses_path}" %>">Payment Status</a></li>
								<li><a href="<%= "#{customers_path}" %>">Customer</a></li>
								<li><a href="<%= "#{pregnancies_path}" %>">Pregnancies</a></li>
								<li><a href="<%= "#{matings_path}" %>">Matings</a></li>
								<li><a href="<%= "#{litters_path}" %>">Litters</a></li>
								
								<li><a href="<%= "#{}" %>">Users</a></li>
							</ul>
			          </li>
		        	<% if current_user && current_user.admin? %>
			          <li class="dropdown">
			              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Management <b class="caret"></b></a>
							<ul class="dropdown-menu">
								<li><a href="">Inventory</a></li>
								<li><a href="">Inventory Records</a></li>
								<li><a href="<%= "#{all_users_path}" %>">Users</a></li>
							</ul>
			          </li>
		        	<% end %>
		        </ul>
		        
		        <ul class="nav pull-right">
		          <% if current_user %>  
		              <li class="dropdown">
		                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Logged in as <img src="<%= current_user.avatar%>" class="avatar"></img> <%= current_user.display_name %><b class="caret"></b></a>
		                <ul class="dropdown-menu">
		                  <li><%= link_to 'My Dashboard', dashboard_path(:user_id =>current_user) %></li>
		                  <li><%= link_to 'Sign out', destroy_user_session_url(), :method => :delete %></li>  
		                  <li><%= link_to "Profile", edit_user_registration_path(current_user) %></li>
		                </ul>
		              </li>
		          <% else %>
		              <li><%= link_to 'Sign up', new_user_registration_url() %></li>
		              <li><%= link_to 'Sign in', new_user_session_url() %></li>
		          <% end %>
		        </ul>
		        <%# if current_user && current_user.admin? %>
		        <% if current_user %>
		        <div class="nav pull-right">
		        	<%= form_tag inventory_records_path, :id=> "search-form", :class => "navbar-search pull-right", :method => :get do %>
		        	<%= text_field_tag :search, nil, :class => "search-query", :placeholder => "Search..." %>
		        	<% end %>
		        </div>
		        <% end %>  
		        <!-- 
		 			<form class="navbar-form pull-right">
		              <input class="span2" type="text" placeholder="Email">
		              <input class="span2" type="password" placeholder="Password">
		              <button type="submit" class="btn">Sign in</button>
		            </form>
		         -->
	      </div><!--/.nav-collapse -->
		</div>
	</div>
</div>